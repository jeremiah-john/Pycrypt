#!/usr/bin/env python
import random

def encrypt():
        key = [];

        messageIn = raw_input("what is it that you want to encrypt?:");
        sliced = list(messageIn);

        for i in sliced:
            print 1;

        return
    
encrypt();
